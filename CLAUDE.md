# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repo status

The app runs end-to-end (live GUI + tools, smoke test green). `src/` is the importable Python package (intra-package relative imports, so `src` rarely shows in code); **the component layout, the four OOP hierarchies, and the runtime data flows now live in `ARCHITECTURE.html`** — this section keeps the rules/invariants, not the structural map. `pyproject.toml` builds via hatchling (`packages = ["src"]` + vendored `vendor/*/src/*`); runtime deps PyQt6/requests/numpy/beautifulsoup4/lxml, dev deps pytest/ruff/mypy. The pytest suite is small but real (`tests/`, run `pytest tests/`). `scripts/smoke_deepseek.py` drives a real DeepSeek round-trip through the agent loop and is **mandatory after any change to `core/`, `llm/`, or the tool-call wire format**. `src/cli.py` (`python -m src.cli`, `--offline` / `--show-reasoning`) is the matching REPL on the same loop — the GUI-seam conformance probe (goes through `build_agent()`). One reference offline subclass per hierarchy (`ClockTool`, `EchoLLMProvider`, `StaticSource`, `HelloWorkflow`) lets the loop + tests run without API keys or live PKU endpoints. `src/tools/pku3b.py` is the shared **in-process backend** (graceful `pypku3b` import, a `client_factory` seam, `secrets/pku/{id,password}` loading, error redaction, a submit-URL builder — **not** a Tool subclass); `PKU3bAssignmentsTool` drives `pypku3b.Client.list_assignments()`, surfacing course/assignment ids, raw + ISO-8601 deadlines, completion, descriptions, attachment URIs, and `url`/`submit_url`/`blackboard_content_id` links.

The `src/rag` embedding chain (`Embedder`/`KnowledgeBase`/`KnowledgeSearchTool`) is **superseded by the doc base and no longer registered** — kept as an OOP artifact. `DeanSource`/`CalendarSource` wire through `build_source_registry()`; `MemoryTool` is the persistent preference store (`MemoryStore`, `src/core/memory.py`).

**Memory is folded into responses, not only tool-reachable**: bootstrap shares **one** `MemoryStore` instance between `MemoryTool(store=...)` and `Agent(memory=...)`, and each `Agent.turn()` iteration merges the current entries — via `render_memory_context()` — into the *leading* system message of a snapshot copy. It is never appended as a second system turn (DeepSeek expects one) and never written into `Conversation`, so the block refreshes after a mid-turn `set` yet rendered history stays clean. Two invariants when touching this: keep the store **shared** — a second `MemoryStore` on the same path silently no-ops because it loads once at construction and never re-reads — and keep the block out of `Conversation`.

**Natural-language input becomes memory without a manual key**: `MemoryStore.remember(text)` stores free text under an auto-derived content-hash handle (`note-<sha1>`) — the keyless producer of the *same* entry kind as `set` (no `note`/preference split; `render_memory_context` stays uniform), idempotent on identical text, no semantic overwrite. `MemoryTool` exposes it as the `remember` action; the system prompt tells the agent to prefer `remember` for facts learned mid-conversation (name, location, language, schedule) and reserve `set` for replacing a value under a known key. The dashboard `MemoryDialog` accordingly dropped its key+value form for a single natural-text box → 记住 button (signature stays `(tool, parent)` plus an additive `learner=` — `test_dashboard_dialogs.py` constructs it positionally).

**The dashboard box also does smart LLM extraction**: `MemoryLearnService(llm, store)` (`src/core/memory_learn.py`, exported from `src.core`) is a dedicated *stateless* extraction — prompts the LLM for a JSON array of atomic facts and stores each via `remember` — **not** a detour through `Agent.turn()` (no chat-history pollution / event noise, same result). `MainWindow` builds it from the agent's **own** `llm` + `memory` (shared store, so dashboard- and chat-learned facts coincide) and injects it into `DashboardPanel(memory_learner=...)` → `MemoryDialog(learner=...)`; with a learner the 记住 button runs the split through `run_async` (LLM call) and shows "已记住 N 条", without one (offline) it stores verbatim inline. The JSON parser (`_parse_facts`) **is the feature** — it must tolerate ```code fences``` and prose, or every real reply degrades to one verbatim blob; offline `EchoLLMProvider` yields no JSON → verbatim fallback, no special-casing. Tradeoff: free-text notes don't overwrite (delete the stale one from the list). Tests: `test_memory_{store,tool,agent}.py` — the last proves a learned fact folds forward into the next call and that `build_agent` shares one store), `PKU3bAnnouncementsTool` (posted date + body come **inline** from `pypku3b` — no `announcement show`/date-cache; undated rows are the body-snippet fallbacks pku3b also emits, ~历史-only); and `KimiProvider` (see the LLM section).

Network tools register **online only** in `bootstrap._build_tools()` because they touch the network (login + scraping).

The **doc base** replaces RAG (`src/tools/doc_base.py`): `doc_search` — lexical ranked search over `doc_base/manifest.json` (title/breadcrumb only; empty query browses all) — registers in **every** mode (no index/network). `doc_read` renders a doc's PDF pages via `pdftoppm` and returns them as **`ToolResult.images`**; `Agent.turn()` injects them as a multimodal `user` message (`Conversation.add_user_parts`) **after** all tool results, so the **visual-role brain reads the pages itself** (no PDF→text middleman). `doc_read` registers **only while the active role is `visual`** (text brains can't read images), toggled by `apply_chat_model`; caps 24 pages, `pages` slices longer docs. 文档库 「让 Captain 阅读」 uses the encapsulated **`DocBaseReader`** service (render→ask visual model→text, `build_doc_reader()`, injected like `memory_learner`). `build_agent`/`MainWindow` dropped `enable_knowledge`, `--rag` is gone (**BREAKING: integration contract**).

`MorningBriefingWorkflow` composes `pku3b_assignments` / `pku3b_announcements` / `clock` into a daily briefing, fetching each via a `ToolRegistry` membership check so an unregistered/failed tool is noted and skipped; `success=False` only when no source is reachable.

**Workflows are agent-callable, not just GUI-button-driven**: `Agent.turn()` serializes only the `ToolRegistry`, so `build_agent` (`_register_workflow_tools`) wraps each `Workflow` in a `WorkflowTool` registered into that registry — it enters `to_openai_schema()` and the model starts it via the tool-call loop (GUI `WorkflowWorker` path unchanged). Recursion is safe **only** because workflows invoke tools by explicit name; don't route adapters through a separate registry. Stubs (`HelloWorkflow`) set `agent_callable=False` to stay registered but out of the toolset.

GUI threading model (three `QThread`s, contract §2) + the 2-panel `dashboard | chat` layout + PKU-red theme (`src/ui/styles.py`): mapped in `ARCHITECTURE.html` §1/§3.

Backend streaming extensions: `LLMProvider.stream_chat()` + `ChatStreamEvent` (default falls back to wrapping `chat()`; `DeepSeekProvider` implements real SSE) and `PKU3bCourseTableTool` (drives `pypku3b.Client.get_coursetable()` → raw portal JSON, parses HTML-wrapped course names, supports OTP).

**plib/dean/treehole/pypku3b are all vendored** under `vendor/` (git subtree → top-level packages `plib_cli`/`dean`/`treehole`/`pypku3b` via the pyproject hatchling `packages` mapping) and driven **in-process** — no sibling `.venv`/subprocess, **no external binary** (standalone). Three credentialed network tool families: `PLibMaterialsTool` (`src/tools/plib_materials.py` — drives the vendored `plib_cli` via `PlibClient`; passes `secrets/plib/{email,password}` as `Credentials` per call so search/quota/download self-authenticate); `TreeholeUpdatesTool` + `TreeholeAuthService` (`src/tools/treehole_updates.py` — `import treehole`, reads `secrets/treehole/{id,password}`, surfaces login/SMS-verify); and the three pku3b tools (below), which drive `pypku3b.Client` with `secrets/pku/{id,password}` (IAAA), a cookie jar at `secrets/pku/cookies.json`, and a crawl cache under `data/pku3b_cache/`. `pypku3b` pins TLS to the **OS trust store** (certifi rejects `course.pku.edu.cn`'s GlobalSign chain). Each vendored lib's upstream is its own repo (`pypku3b`→`RizzoHou/pypku3b`); update one via `git subtree pull --prefix vendor/<lib> <remote> main --squash`, never by editing `vendor/`.

**macOS treehole desktop notifications**: `TreeholeNotificationService` (same module — a non-`Tool` sibling of `TreeholeAuthService`) manages a per-user LaunchAgent `com.pku.captain.treehole.notify` running `treehole monitor --notify` on a configurable `StartInterval`, pointed at pku-captain's own `secrets/treehole` session so one GUI login drives both dashboard and notifier (plist + `launchctl bootout→bootstrap→enable→kickstart`; interval at `data/treehole_notify.json`, default 60s, re-installs on change; state/logs under `data/treehole-notify-*`). GUI: a **消息通知** button in `TreeholeMessagesDialog` opens `TreeholeNotificationDialog`; `launchctl` is stubbed in tests via an injected `runner` (`tests/test_treehole_notify_service.py`). macOS-only, runs pku-captain's **own** venv `treehole` console script (`[project.scripts]`) — inert off-macOS.

The dashboard 树洞消息 card **stays in sync with that auto-checking**. Two parts.

(1) **Accumulate, don't replace**: `monitor.check()` returns only the delta since the dashboard's **own** cursor (`secrets/treehole/state.json` — deliberately **separate** from the notifier's `data/treehole-notify-state.json`; concurrent writes are benign), so replacing per-poll made a reply **vanish on the next empty poll**. Instead a persisted `TreeholeInboxStore` + pure `merge_treehole_updates` (both in `treehole_updates.py`) union by pid/cid into `data/treehole_inbox.json` and keep unread visible until the user opens `TreeholeMessagesDialog` (mark-as-read → `clear`); the inbox is **in-memory by default** (`path=None`, so tests + offline never touch `data/`), injected into `DashboardPanel(...)` by `MainWindow`. `set_treehole_updates` / `set_error` merge-and-render-from-inbox rather than overwrite, so a transient poll error keeps the accumulated rows + badge. Sibling **`TreeholeHistoryStore`** (`data/treehole_history.json`, append-only, per-comment by (pid,cid)) takes the **same raw poll** but is **never cleared on read** — backs the dialog's 历史消息 tab (newest-first, + 清空历史); feed from the poll, not the post-clear inbox (count-only polls / per-poll `limit` are gaps).

(2) **Auto-refresh on the notifier's cadence**: a `MainWindow` `QTimer` re-polls the card while notifications are enabled+supported+**logged-in** (`_reconfigure_treehole_sync`, re-read on startup + on `treehole_settings_changed` when the dialog closes), via a **quiet**, busy-guarded `run_async` poll (no flicker/spam). Limits: the card re-derives via its *own* poll, not the daemon's Notification-Center list (count-based trigger can drift); app-closed replies still surface on reopen because the cursor is frozen while shut. **Don't** unify the (now three) state files or revert accumulation to replace (re-introduces the two-poller race / vanish-on-poll bugs). Tests: `test_treehole_inbox.py` / `test_treehole_history.py` (stores) + `test_treehole_sync.py` (accumulate / mark-read / history-survives-clear / error-preserve, timer gating).

Tool calls render inline via `InlineToolCall` (`ToolTracePanel` retained-but-unmounted); dashboard dialogs for treehole/P-Lib/announcements/memory/文档库.

**Dashboard dialogs follow integration-contract §1+§5**: they obtain tools from the injected `ToolRegistry` (`tools.find(name)`, `name in tools`) — never by constructing `Tool` subclasses — with online-only entries gated on registry membership (so offline never touches a network/subprocess tool), and every blocking dialog call goes through `src/ui/tool_call_worker.py`'s `run_async()` onto a `QThreadPool` to keep the window responsive.

**Dashboard refresh is scoped, not global**: the header 刷新 button fires the panel's `refresh_requested` (reloads all cards), but each card's own 刷新 button (and a post-login `auth_changed`) must emit `partial_refresh_requested([tool_key])` so `DashboardWorker.refresh(keys=...)` reloads only that card — wiring a card's refresh to the global `refresh_requested` reintroduces the "one click refreshes everything" bug.

**Dashboard refresh is also parallel, not serial**: `DashboardWorker.refresh` fans the selected tools out over a `ThreadPoolExecutor` and emits each card's result via `as_completed` from the worker thread, so startup time is the slowest single card, not the sum. Concurrent refresh is safe by **error isolation**: `_invoke` swallows every exception and each card reports independently via `item_error`, so one card's failure never crashes the dashboard or corrupts a sibling. The one shared resource within a refresh is pypku3b's crawl cache (`data/pku3b_cache/`): on a cold/stale session `pku3b_assignments` + `pku3b_announcements` share the `Blackboard::courses`/`menu_<id>` keys and may each fetch + re-login (worst case a redundant refetch); cache + `secrets/pku/cookies.json` writes are atomic (`os.replace`) so concurrent same-key computes can't tear a file. Creds injection is concurrency-safe — each pku3b tool (like `PLibMaterialsTool`) builds a fresh `pypku3b.Client` per call, no shared mutable state. `tests/test_dashboard_worker_refresh.py` guards this with a `threading.Barrier` test.

**Dashboard cards are cached to disk for instant reload**: `DashboardCache` (`src/core/dashboard_cache.py`, built via `build_dashboard_cache()`, lazily re-exported) saves each card's **raw tool payload** to `data/dashboard_cache/<tool_key>.json` — one dumb, lock-guarded file per card, mirroring `SessionStore` (no in-memory mirror; the GUI owns change-detection). `MainWindow` paints from `load_all()` at startup (`_seed_dashboard_from_cache`), then fires a **silent** refresh (`_start_refresh([], silent=True)`) that suppresses the spinner on already-cached cards and repaints only the ones whose data changed — a canonical-JSON `_signature` compare, with `_should_render` enforcing the invariant that a card put into the loading state (tracked in `_loading_keys`) always renders so it can never strand on the spinner. Explicit header/per-card refresh stays non-silent (always spins + repaints, for click feedback). A refresh error keeps the last-good cached card visible with a status-bar note rather than wiping it to an error — so an offline launch shows the prior online cache (deliberate; the `离线模式` subtitle signals it). Caching **raw** payloads (not rendered widgets) means time-relative renders like `upcoming_assignments` re-filter against today's clock on repaint. `tests/test_dashboard_cache.py` covers the store plus the pure `_signature` / `_should_render` decision (full MainWindow wiring validated headless, not committed).

**Chat renders one bubble per assistant segment, not per turn**: a tool-using turn runs multiple LLM iterations, each emitting an assistant text segment then tool calls, so `MainWindow._on_agent_event` calls `ChatPanel.finalize_assistant_segment()` on every `tool_call` to lock the current streaming bubble in place before the tool row — without it, later segments overwrite the first bubble (the `final` event's `add_assistant_message` rewrites it) and the last reply renders above its tool calls instead of below; `tests/test_chat_panel_segments.py` drives real `AgentEvent`s through `_on_agent_event` (offscreen Qt) to guard the ordering.

**Chain-of-thought ("thinking") is optionally visible, off by default**: `DeepSeekProvider.stream_chat` yields `ChatStreamEvent(reasoning_delta=...)` per token (+ accumulating `reasoning_content` for replay); `Agent.turn()` forwards an additive `reasoning_delta` AgentEvent (contract §3; before `assistant_delta` per segment) → `ChatPanel.append_reasoning_delta`. A checkable `ChatPanel` header button flips **💭 思考可见**↔**思考不可见** (`_show_thinking` defaults False). **When off, reasoning_delta is dropped outright — no hidden widgets are built.** When on, each segment renders in an `InlineThinking` widget — a read-only sliding-window `QPlainTextEdit` that auto-sizes width/height from font metrics, measured **after `ensurePolished()`** so the late-applied `#InlineThinkingBody` QSS font is used (else the first window renders too tall), **not** `documentSize` (stale after a width change). Two ordering rules: the window must **finalize before its tool row** — finalization fires from `_finalize_streaming` and the first answer token, not only the `final` event, else a tool-only iteration locks in late — and `append_reasoning_delta` **collapses earlier windows** when a new segment starts (bounds the per-segment stack). `load_history` replays persisted `reasoning_content` (when on) as a finalized window before its bubble; offline `EchoLLMProvider`/`thinking=False` emit none. `tests/test_chat_panel_thinking.py` covers orderings, toggle, history replay, first-window auto-fit.

**macOS Calendar DDL notifications**: `CalendarReminderTool` (`src/tools/calendar_reminder.py`) shells to `osascript` to write selected assignment deadlines into a dedicated **PKU Captain** Calendar.app calendar (create-if-missing) as events with a `display alarm`. macOS-gated (graceful `ToolResult` error elsewhere), surfaces TCC Automation-denied (`-1743`) as an actionable Chinese message (and short-circuits the batch on denial), registers **online-only**. AppleScript gotchas in the script: pass fields as `osascript -` argv (no shell/CJK escaping); build the date from numeric components (set `day` to 1 first to dodge month-overflow); put `end date` in the `make new event` properties (a later `set end date` fails `-10025`). Dashboard: a 加入日历 button on the 近期 DDL card (`AssignmentTodoCard`) opens `CalendarReminderDialog` (future deadline-bearing assignments seeded from the card, alarm-offset dropdown; add via `run_async`), gated on `calendar_reminder` membership. `tests/test_calendar_reminder_tool.py` mocks `subprocess.run` + `sys.platform`.

**Multi-session chat** persists + reopens via a **＋新对话** / **历史会话** header pair (`SessionHistoryDialog` = modal open/delete list). `src/core/session_store.py` `SessionStore` writes one `data/sessions/<id>.json` (full history incl. `tool_calls` + `reasoning_content`, title, timestamps) per turn-finish and on close — but **only after the first real user turn**, so an untouched window leaves no junk file. `src/core/session_titler.py` `SessionTitler` auto-names from the opening exchange via a `deepseek-v4-flash` provider in **non-think mode**, async (`run_async`); offline/key-less → a network-free `SessionTitler.heuristic` (never `EchoLLMProvider`). The GUI never writes `Conversation` directly: `bootstrap` adds `build_session_store` / `build_session_titler` / `reset_conversation` / `restore_conversation`, and `Conversation.load_messages` replaces history in place (keeps the `AgentWorker`-held object identity). Two guards: (a) session switching no-ops while a turn is in flight (`MainWindow._busy` + disabled header buttons) so the conversation isn't swapped mid-append; (b) `drop_incomplete_tool_calls` trims a trailing `assistant(tool_calls)` whose results are missing — a turn aborted mid-dispatch would otherwise persist an invalid sequence and **400 forever on reopen** — run on both save and restore. The async titler writes via `SessionStore.update_title` to the **captured** session id so a late result can't mis-stamp a switched-away session. v1 limit: restored tool rows show a stringified blob, not the structured render. Resolves integration-contract §6; tests `test_session_{store,titler}.py` + `test_chat_panel_history.py`.

`DeanResourcesTool` (`src/tools/dean_resources.py`) drives the vendored `dean` library in-process (`dean.resources.*` via a shared `fetch_dean()` seam; see `docs/setup_zh.md`). Fetches **public** dean.pku.edu.cn resources — **no credentials/login** (unlike plib/treehole). Single `action` enum flattening the CLI's nested subcommands — a **read** surface (`sidebar`/`guide`/`rules_list`/`rules_show`/`notice_list`/`notice_show`/`download_list`/`openinfo_list`; ids/scope/paging in the schema) plus **file-download** actions `download_get`/`openinfo_get` (save by `id`/`ids` into `downloads/dean/<kind>`, gitignored; `output_dir` overridable; 180s timeout). Deferred: `--all` paging **omitted** (timeout/flood risk) — page via `page`, read `last_page`. Registers **online-only**. `tests/test_dean_tool.py` fakes `dean.resources.*` (both dean tools take an injectable `client_factory`).

`DeanUpdatesTool` (`src/tools/dean_updates.py`, online-only) returns the full current snapshot `items` (all sources incl. **通知公告** `notice`); the GUI accumulates it into a never-lossy `DeanInboxStore` (`data/dean_inbox.json`, pure `merge_dean_updates` by `key`+earliest `first_seen`) and paints a **render-time recency window** (`split_dean_items`: notices 1mo / others 6mo, date-less rules by `first_seen`). The 教务更新 card **title** opens `DeanMessagesDialog` (新消息/历史消息 tabs × per-source columns, no data loss); notice/rule rows → `DeanDetailDialog` (full text via `notice_show`/`rules_show` + Safari). The `seen` baseline now feeds only the agent's `new_count`, never the GUI. Tests `test_dean_{inbox,sync}.py` + `split_dean_items`.

`TreeholeTool` (search/fetch in `treehole_updates.py`) is an **agent tool with no GUI entry** by design.

`bootstrap._sync_pku3b_identity_memory` folds `pypku3b.Client.get_identity()` into `MemoryStore` at startup — **sync-once** (skips the portal login once `identity.*` keys exist), skips when `secrets/pku` is empty, injectable `client_factory` for tests, stores only the public summary (no 身份证号/住址); `PKU3bCourseTableTool` parses `备注：`→`CourseBlock.note`. Tests: `test_pku3b_identity_memory.py` / `test_pku3b_tools.py`.

Keep the "one offline reference, real ones for teammates" pattern when adding more scaffolding.

**Change history lives in `CHANGELOG.md` — add an entry there per merge, don't grow this section.**

## Quickstart

```bash
python3 -m venv .venv && source .venv/bin/activate
pip install -e ".[dev]"
python -m src                  # PyQt6 window — defaults to offline (Echo LLM, offline tool subset)
python -m src --online         # PyQt6 window — text model + live tools; configure keys in the GUI 账号中心 (or secrets/models.json / legacy deepseek_key.txt; doc_read needs the visual/Kimi role)
python -m src.cli --offline    # interactive REPL (no API key; EchoLLMProvider)
python -m src.cli              # interactive REPL against the real DeepSeek API
find src -name '*.py' -print0 | xargs -0 python -m py_compile  # syntax check
ruff check src                 # lint
pytest tests/                  # unit tests
python scripts/smoke_deepseek.py  # real DeepSeek round-trip — mandatory after core/llm/wire-format changes
```

The GUI defaults to offline so first-time clones don't hit the network. Chat LaTeX needs the **optional** `math` extra (`pip install -e ".[dev,math]"`, PyQt6-WebEngine); without it bubbles stay QLabel (LaTeX shown as source). **Don't make WebEngine a hard dep** (`chat_panel` gates it on `_WEBENGINE_IMPORTED`).

### Worktrees (`claude --worktree`)

`.worktreeinclude` (gitignored, per-machine) copies only *gitignored* paths git won't carry over (tracked files like `CLAUDE.md` come via git). It covers `secrets/` (API keys + P-Lib/treehole/pku creds + sessions/cookies), plus `.claude/*` + `!.claude/worktrees/` so a worktree gets local Claude config but not the 24 MB worktrees dir — use `.claude/*` not `.claude/`, else git prunes the dir before the `!`. (No `.local/` — pku3b is now the vendored in-process `pypku3b`.)

`.venv/` is **not** copied (~1.2 GB per worktree). Worktrees share the main venv via a symlink — run once inside the worktree (its `.claude/worktrees/<name>/` location puts the repo root three levels up):

```bash
ln -s ../../../.venv .venv
```

The shared venv's editable pointer names the **main** checkout, so a bare `import src` from a worktree resolves to main; the repo-root `conftest.py` front-loads the session's own root first so `pytest tests/` imports the worktree's `src/` (`python -m src` / `scripts/smoke_deepseek.py` already front-load via `-m` / `sys.path.insert`). Don't `pip install` from a worktree — it mutates the shared venv (leaks to main + siblings); install in the main checkout. `.gitignore` is `.venv` (no slash) so the symlink is ignored too.

REPL `/save [path]` dumps the conversation (incl. `reasoning_content`) to `debug/conversation-<ts>.json` (gitignored) — handy for thinking-mode wire-format regressions (preserves the exact assistant→tool→assistant ordering the endpoint validates).

**Credentials are entered via the GUI 账号中心 (`src/ui/login_dialog.py`, one 账号 button, tabbed 统一身份·树洞 / P-Lib / 模型配置), not hand-placed files.** `CredentialStore` (`src/core/credentials.py`, exported from `src.core`) is the single `secrets/` writer/status/clear; tools keep reading the same files. Layout (`secrets/` gitignored) — `secrets/models.json` (per-role `{api_key,base_url,model}`), `secrets/plib/{email,password}` (login page now **persists** them; the old `PLibLoginDialog` only validated → lost on restart, now removed), `secrets/treehole/{id,password}` (IAAA; SMS→`session.json`); legacy `secrets/api_keys/{deepseek,kimi}_key.txt` stay an api_key fallback (no migration) — notes in `docs/setup_zh.md`. **Chat brains are two configurable roles, not brands**: `text` (文本模型, default DeepSeek) and `visual` (视觉模型, default Kimi, vision) — each reads endpoint/model/key from `CredentialStore`, so any OpenAI-compatible endpoint works. `src.core`'s `available_chat_models`/`build_chat_llm`/`apply_chat_model`/`DEFAULT_CHAT_MODEL`(=`text`) + the header switcher key off the **role** (`text`/`visual`); provider `.name` stays `deepseek`/`kimi`. A switch **resets the chat (reset-on-switch)**, gates `doc_read` on the visual role, and the context meter auto-adapts via `llm.context_window` (1M⇄256k). Model edits apply next launch; the page works **offline** (first-run diagnostic points to 账号). **`KimiProvider` is a chat brain** (SSE `stream_chat`, thinking via `{"thinking":{"type":"enabled"}}`→`reasoning_content`, replay **optional** unlike DeepSeek, tools, usage). `VisionRouter` auto-switches a 培养方案/文档 question from the text role into a fresh visual-role chat; `session_store` collapses injected image `user` messages to a text label on persist. **Both providers' `stream_chat` iterate raw bytes then UTF-8-decode** — `iter_lines(decode_unicode=True)` corrupts multi-byte CJK `reasoning_content` at chunk boundaries (don't revert). `DeepSeekProvider` defaults to model `deepseek-v4-pro` with reasoning `effort=max` — these are deliberate; don't substitute `deepseek-chat` or weaken the effort even if a stale model error tempts you to "fix" it, confirm with the captain first. The effort is sent on the wire as the `reasoning_effort` body key (the field name must stay `reasoning_effort` — a top-level `effort` is silently ignored; valid: `high`/`low`/`medium`/`max`/`xhigh`). The thinking-mode endpoint **requires** every previous assistant turn's `reasoning_content` to be replayed on the next request (otherwise: `400 invalid_request_error`); this is wired through `ChatResponse → ChatMessage.reasoning_content → DeepSeekProvider._to_api_message`. Preserve that round-trip when touching any of the three. The `deepseek-v4-flash` channel (session auto-naming) runs **non-think**: `DeepSeekProvider(thinking=False)` drops `reasoning_effort`, sends `{"thinking":{"type":"disabled"}}`, returns plain `content`. `thinking=True` (default everywhere else) keeps the historical body — don't "simplify" the `_build_body` branch.

## What this project is

**PKU Captain** is a desktop AI assistant for PKU students, submitted as a course assignment for an OOP / programming-practice class. Three-person team. The product is a PyQt6 desktop app whose **end-state center of gravity is the dashboard** — a single-screen "信息总站" surfacing today's classes, near-term DDLs, and course notices, with an extensible `Source` registry meant to absorb more PKU information sources over time (教务网 / 树洞 / 公众号 as post-06-06 additions). A side-panel **LLM agent** handles natural-language questions about that same data (via `pku3b`, etc.) and orchestrates multi-step workflows. Note: Week 1 is built **agent-first** as a vertical slice to prove the four OOP hierarchies — that's a build-order choice, not the end-state UX. See `docs/design_reference_zh.md` for the full vision and `docs/roadmap_zh.md` "产品定位（终态）" for the dashboard-vs-agent ordering call.

## Project docs and their authority

- `docs/design_reference_zh.md` — design **reference**, not a contract. The team is explicitly not bound to its architecture, class hierarchy, or tech-stack choices. The **only** part treated as binding is the **"核心功能" (core features)** section, which lists the 6 features the product must deliver:
  1. Unified dashboard
  2. Conversational sidebar with visible tool calls
  3. Tool set (pku3b, doc base find/read, memory, reminder — lectures cut 2026-06-06 as redundant; weather cut 2026-06-04; both kept in `design_reference_zh.md` 核心功能 as struck-through, justified deferrals)
  4. Multi-step workflows (morning briefing, weekly review, course catchup)
  5. Auto-refreshing RAG knowledge base over PKU authoritative sources (now the split-PDF **doc base** + vision read, not embeddings)
  6. Persistent personal-preference memory
- `docs/schedule_zh.md` — the course schedule, which **is** load-bearing:
  - **2026-06-06** — initial version + screen-recording demo (next hard deadline)
  - 2026-06-10 / 06-12 — in-class roadshow (bonus)
  - 2026-07-06 — final GitHub submission (graduating-year teams: 2026-06-28)
- `docs/roadmap_zh.md` — the team's working plan, derived from the design doc + schedule. Weekly milestones, cut-list, hard checkpoints, risk register. Update this when scope changes; never refactor it silently.
- `docs/integration_contract_zh.md` — **load-bearing**. Defines the public API surface, threading model (`AgentWorker` on a `QThread`), event-stream contract, and error contract between the backend lanes (`src/core`, `src/llm`, `src/tools`, `src/workflows`, `src/rag`) and the PyQt6 GUI lane. The seam's entry point is `src.core.build_agent` (defined in `src/core/bootstrap.py`); GUI code constructs `Agent` only through that factory and must not import concrete `LLMProvider` / `Tool` subclasses directly. `build_agent` / `build_source_registry` are re-exported from `src/core/__init__.py` lazily via PEP 562 `__getattr__` — `bootstrap` imports the full tool set and some tool modules import `src.core.memory`, so an eager re-export reintroduces a `core`↔`tools` import cycle; keep the lazy form. `build_agent(offline=True)` swaps in `EchoLLMProvider` + the offline tool subset for GUI work without an API key. The dashboard obtains its `SourceRegistry` the same way — through the `src.core.build_source_registry` factory (same module) — never by constructing `Source` subclasses directly. `src.llm` also exports `ChatStreamEvent` + `TokenUsage`; `Agent.turn()` yields `assistant_delta` / `reasoning_delta` / `context_usage` beyond `llm_response` / `tool_call` / `tool_result` / `final` (see contract §3), and takes an optional `cancelled` predicate (cooperative turn interruption); `AgentWorker.request_cancel` flips a `threading.Event`, **not** a `@pyqtSlot` (worker blocked in-turn). Read the contract before touching either side; breaking changes need a `BREAKING: integration contract` PR note so captain re-reviews.
- `docs/setup_zh.md` — environment setup for teammates (Python venv, `secrets/pku/{id,password}` for the in-process `pypku3b` client, first-run IAAA login/OTP, DeepSeek/Kimi key paths). No longer documents a Rust `pku3b` install — that dependency is gone.
- `docs/tasks/` — task-delegation package for backend work farmed out to `git worktree` Claude sessions. `000_delegation_guide.md` is captain-facing: how to spawn worktrees (`claude --worktree <name> "..."` — the name is mandatory), the two-wave dependency order, sequential merge order, and the additive-conflict set (`bootstrap.py`, the `__init__.py` files, `pyproject.toml`). `NNN_*.md` are standalone task briefs a worktree Claude reads to do one task in isolation (commit to its branch, no push/merge). Currently holds the Week-2 backend set (001–008), all merged into `main` as of 2026-05-22. When delegating or integrating worktree-farmed work, start here.

Plan scope so the 06-06 demo is achievable; polish belongs in the 06-06 → 07-06 window.

## When you write code here

- **Stack** is now committed: Python 3.11+ / PyQt6 / switchable 文本/视觉 model roles (default DeepSeek / Kimi, endpoints user-configurable; vision doc_read on the visual role) via `LLMProvider` abstraction / **`pypku3b`** in-process PKU 教学网/门户 client (vendored) — the doc base replaced the embedding RAG.
- **OOP is the point of the course.** New tools, workflows, LLMs, or sources go in as subclasses of the existing ABCs in `src/{tools,workflows,llm}/base.py` and `src/rag/source.py`, and register against the matching `*Registry` at the call site (keep modules side-effect-free — no auto-registration at import). Resist ad-hoc dispatch — the "subclass + register" pattern is the rubric showcase.
- **Default to English** — interaction, operational docs (`docs/tasks/`), commit messages, in-code comments, code identifiers. **Chinese** is reserved for end-user-facing product copy (the app's users are PKU students) and the existing `*_zh.md` design docs (`design_reference_zh`, `roadmap_zh`, `schedule_zh`, `integration_contract_zh`, `setup_zh`) — don't translate those unprompted.

## Team and coordination

Three members; the user (`RizzoHou` on GitHub) is captain and absorbs orchestration overhead. Teammates: `by-lastime`, `Q-star17`. Suggestions that touch division of labor should be phrased so the captain can hand them off — don't assume the user will personally do every task.

Captain delegates coding to Claude rather than typing personally. That changes a few things: (a) per `docs/roadmap_zh.md` 协作约定, weekly tasks are an open-claim checklist — no pre-assigned lanes; any contributor picks up an unchecked item and appends their GitHub handle as involver on completion (reviewers / later editors append too); (b) when scaffolding, write base classes + at most one reference subclass per hierarchy, leaving the rest for teammates to pick up so they can defend their own work at the roadshow; (c) captain reviews teammate PRs via the `/review` CLI skill (no automated GitHub Action was chosen).

**No lane split** (the old v1 backend/GUI split is superseded): any contributor may work any lane, GUI included — never defer a change as "the GUI lane's job". The integration contract remains the seam to respect when crossing GUI ↔ backend.

`CLAUDE.md` is tracked in git so `git worktree` sessions (which don't copy gitignored files) inherit project context — a team-visible, public-GitHub artifact: keep coordination notes factual and captain-private framing out. Team-facing prose docs live in `docs/*.md`.

## Agentic auditing workflow

Three repo-root artifacts keep the project auditable by a non-coding architect — verification relocated off the code onto stack-agnostic prose + observable behavior (seed: `docs/external/pku-captain-agentic-workflow-handoff.md`). Each is maintained by a same-named project-level skill in `.claude/skills/` (tracked via the `.gitignore` un-ignore, so teammates/worktrees inherit them); the SKILL.md files hold the detailed rules. Fire each **by change type**, not every task:

- `ARCHITECTURE.html` — structural map (skill `architecture`, structural changes only). *Structure* here; *rules* in "Repo status"; *seam* in `integration_contract_zh.md` — cross-reference, never restate.
- `DEVCHANGELOG.md` — decision log, why X over Y (skill `devchangelog`, ~every non-trivial change). *Why* here; *what-shipped* in `CHANGELOG.md`.
- `VERIFICATION.md` — human-run verification steps, Pending→Verified (skill `verification`, user-visible / release-critical / beyond-`pytest` changes).

`TASTES/` — a peer prose artifact on a different axis (*how code should look/feel*): prescriptive coding-taste guidance (README + four broad topic files), maintained by the same-named `tastes` skill when a session surfaces a reusable taste lesson. Codebase-first; cross-references CLAUDE.md (rules) / DEVCHANGELOG.md (decisions) / ARCHITECTURE.html (structure), never restates.

Experimental machinery — to be packaged as a Claude plugin once mature.

**Plan-gate.** For any non-trivial/structural change, state the plan in plain prose *before* coding — files, approach, what could break, assumptions — and let the architect approve the **plan, not the diff**. Push boundary decisions (new service, new global) UP into the plan; leave idioms (which Qt signal, which API call) DOWN in code. Trivial changes (typo, one-liner, test tweak) skip it. Narrower than the `claude-md-improver` auto-apply waiver below, which covers only this file.

**Credentials** leak invisibly to behavior tests (the app works while leaking) — audit by hand before release; the live stderr→`ToolResult.error`→LLM-context risk and its checks are in `VERIFICATION.md` "Credential pre-release audit".

## Conventions worth observing

- Conventional Commits for git messages (per user's global rules).
- Log every merge in `CHANGELOG.md` (Keep a Changelog format, newest first). The Repo-status section is for current invariants, not change history; the structural map is `ARCHITECTURE.html`; per-change decisions go in `DEVCHANGELOG.md` (see "Agentic auditing workflow" for the skills + change-type gating).
- Markdown prose paragraphs are written as single unbroken lines — do not hard-wrap at 80 chars when editing files here.
- PDFs generated from source must be visually audited via the `Read` tool, not only by inspecting the LaTeX/Typst input.
- **Apply CLAUDE.md changes without explicit approval (this project only).** When the `claude-md-improver` skill reaches its "ask for confirmation" gate, skip it and apply the proposed edits directly. Still produce the quality report and show the diff so the user can review after — pre-approval is waived, transparency is not. Applies only to this repo's `CLAUDE.md` / `.claude.local.md`; other files keep normal approval behavior.
- GitHub repo settings: squash-merge-only, auto-delete head branches on merge. CODEOWNERS routes PRs to captain by default; lane-specific routing is commented out until the Week 0 skills conversation locks assignments. Branch protection is on the GitHub Pro plan; captain will apply for the Student Developer Pack to unlock it before crunch time.
